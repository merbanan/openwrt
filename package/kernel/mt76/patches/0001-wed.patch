diff --git a/mt7915/main.c b/mt7915/main.c
index 282eb5e3..e036fed2 100644
--- a/mt7915/main.c
+++ b/mt7915/main.c
@@ -1372,12 +1372,12 @@ mt7915_net_fill_forward_path(struct ieee80211_hw *hw,
 	if (msta->wcid.idx > 0xff)
 		return -EIO;
 
-	path->type = DEV_PATH_MTK_WDMA;
+	path->type = DEV_PATH_WDMA;
 	path->dev = ctx->dev;
-	path->mtk_wdma.wdma_idx = wed->wdma_idx;
-	path->mtk_wdma.bss = mvif->mt76.idx;
-	path->mtk_wdma.wcid = msta->wcid.idx;
-	path->mtk_wdma.queue = phy != &dev->phy;
+	path->wdma.wdma_idx = wed->wdma_idx;
+	path->wdma.bss = mvif->mt76.idx;
+	path->wdma.wcid = msta->wcid.idx;
+	path->wdma.queue = phy != &dev->phy;
 
 	ctx->dev = NULL;
 
